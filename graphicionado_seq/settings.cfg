{
	//Set all vertices as active the entire time(e.g page rank algorithm)
	$isAllVerticiesActive:[y] //Options: y, n
	
	//For the future is not implemented yet
	//convergenceThreshold:""

	//Choose a limit of iterations possible on the graph
	$maxIteration:[0] //Options: 0 (0 is infinity), any value e.g 42

	//Choose if double of same edges should be allowed.
	$edgeDuplicate:[y] //Options: y, n
}